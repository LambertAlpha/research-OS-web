# Folder: liquidity

1. **地位**: 流动性模型 v3.0 专属页面。深度展示美元流动性监测结果，包括评分、分项得分、禁止策略、流动性指标图表。
2. **逻辑**: 接收用户"运行模型"操作 → 调用 `apiClient.runModel()` → 解析 `ModelOutput.liquidity` (LiquidityOutput) → 并发获取 WALCL/SOFR/IORB/WRESBAL/RRPONTSYD/MOVE 市场数据 → 渲染卡片与图表。
3. **约束**: 仅关注流动性维度数据，不渲染宏观模型内容；图表数据需在页面层做单位换算（如 /1e6 转万亿）。

## 成员清单

- `page.tsx`: 流动性模型页面组件 (Page Component) — 路由 /liquidity

**触发器**: 一旦本文件夹增删文件或架构逻辑调整，必须重写此文档。
